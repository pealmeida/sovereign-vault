{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for Sovereign Vault desktop windows.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-confirm"
  ]
}
