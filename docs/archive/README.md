# Archive

Historical, point-in-time documents kept for provenance. **These do not reflect
the current state of the project** and are not maintained.

- `analysis/` — early positioning/strategy and reference-vault analysis
  (2026-05-23). Some "current state" claims are now superseded by shipped code.
- `superpowers/` — the completed three-page UI-redesign plan/spec (2026-05-22).
  The work it describes already shipped in `ui/`; the absolute paths in it are
  from a prior machine.
- `e2e-results-2026-05-25.md` — a live end-to-end test run captured on a Windows
  build. See `docs/testing/` for the current, platform-neutral test plan/cases.

For current docs see [`../`](../): `ARCHITECTURE.md`, `threat-model.md`,
`USAGE_REAL.md`, `GETTING_STARTED.md`, the ADRs, and `testing/`.
